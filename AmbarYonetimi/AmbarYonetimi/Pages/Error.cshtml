@model AmbarYonetimi.Models.ErrorViewModel
@{
    ViewData["Title"] = "Hata";
}

<h1 class="text-danger">Bir Hata Oluştu</h1>

@if (!string.IsNullOrEmpty(Model?.RequestId))
{
    <p><strong>İstek ID:</strong> <code>@Model.RequestId</code></p>
}

<p class="text-muted">Bir sorunla karşılaşıldı. Lütfen daha sonra tekrar deneyin.</p>

